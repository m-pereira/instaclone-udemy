<%= turbo_frame_tag "#{@resource.class.name.downcase}-#{@resource.id}-likes" do %>
  <%= button_to '', likes_path(likeable_type: @resource.class, likeable_id: @resource.id),
                class: "#{@resource.class.name.downcase}-actions-like" %>
<% end %>
